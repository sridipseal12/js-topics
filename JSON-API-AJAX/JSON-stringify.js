const user = { name: "Bob", age: 30, city: "London" };

// Convert JS object into JSON text
const jsonText = JSON.stringify(user);

console.log(jsonText);
// Output: {"name":"Bob","age":30,"city":"London"}
